"""Stub for Phase 2 webhook subscriptions implementation.

TODO(phase-2): Define the WebhookSubscription SQLAlchemy model here. Will store
per-org webhook endpoints (target URL, signing secret, subscribed event types,
delivery status) and back the outbound delivery worker. Phase 2 agent will
also wire it into ``backend/app/models/__init__.py``.
"""
