import { V4SectionStub } from "./v4-section-stub";

export function V4Artifact() {
  return (
    <V4SectionStub
      tag="IMG·08"
      label="PHASE 6 · ARTIFACT / SIGNED PDF"
      caption="Artifact section — verification recipe and Signed PDF artifact land in phase 6."
    />
  );
}
