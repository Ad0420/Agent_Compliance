import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // TriageGuard demo frontend — runs on :3002 to avoid collisions with the
  // ScribeMD demo (:3001) and the Vera landing (:3000).
};

export default nextConfig;
